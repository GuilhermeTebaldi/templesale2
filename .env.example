# Required in all environments.
# Use the Render External Database URL for local dev.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE

# Set to "false" only for local Postgres without SSL.
PGSSL=true

PORT=5173
